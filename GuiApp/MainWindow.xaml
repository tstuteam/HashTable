<Window x:Class="GuiApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuiApp"
        xmlns:viewmodel="clr-namespace:GuiApp.MVVM.ViewModel"
        mc:Ignorable="d"
        Title="Phone Book" Height="450" Width="800"
        Background="#ffffff">

    <Window.DataContext>
        <viewmodel:MainViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0"
              Background="#f00">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>

            <TextBox Grid.Row="0"/>

            <Label Content="Контакты"
                   Grid.Row="1"/>

            <ListView Grid.Row="2"
                      ItemsSource="{Binding Contacts}"
                      ItemContainerStyle="{StaticResource ContactCard}"/>

            <Button Content="Импортировать контакты"
                    Grid.Row="3"/>
        </Grid>
    </Grid>
</Window>
